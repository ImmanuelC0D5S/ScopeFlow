# ScopeFlow

AI-powered operational assistant for scope change detection and workflow automation.

## Generate handoff

```powershell
./scripts/create_handoff.ps1
```

This regenerates `handoff.md` from `docs/handoff_prompt.md`.
